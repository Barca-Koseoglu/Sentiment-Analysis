# Sentiment-Analysis
Built a sentiment analysis model using logistic regression to classify text as positive, negative, or neutral. Preprocessed data, vectorized text using TF-IDF, trained and evaluated the model with accuracy, confusion matrix, and classification report metrics.

One more file was used to tune hyperparameters and compare using Optuna hyperparameter tuning with GridSearchCV hyperparameter tuning.
